import { ContactCTA } from '@/components/sections';
import { portfolio } from '@/config/portfolio.config';
export default function ContactPage(){return <section><h1 className="text-4xl font-semibold tracking-[-.03em]">Contact</h1><p className="mt-4 max-w-2xl leading-7 text-secondary">Saya paling cocok membantu saat produk sudah punya masalah nyata: dashboard mulai lambat, flow penting membingungkan, atau komponen UI mulai sulit dirawat.</p><ContactCTA/><div className="mt-8 grid gap-4 md:grid-cols-2">{portfolio.testimonials.map(t=><blockquote key={t.author} className="rounded-3xl border border-line bg-card p-6"><p className="leading-7 text-secondary">“{t.quote}”</p><footer className="mt-4 text-sm"><b>{t.author}</b><span className="text-muted"> · {t.role}</span></footer></blockquote>)}</div></section>}
