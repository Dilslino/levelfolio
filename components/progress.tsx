import { clamp } from '@/lib/utils';
export function XPBar({ current, next }: { current: number; next: number }) { const pct = clamp((current/next)*100); return <div><div className="mb-2 flex justify-between text-xs text-secondary"><span>{current.toLocaleString('id-ID')} XP</span><span>{Math.round(pct)}% menuju level berikutnya</span></div><div className="h-2 rounded-full bg-white/7"><div className="h-full rounded-full bg-gradient-to-r from-accent to-cyan" style={{width:`${pct}%`}} /></div></div>; }
export function ProgressRing({ value }: { value: number }) { const pct=clamp(value); return <div className="grid size-14 place-items-center rounded-full" style={{background:`conic-gradient(#8B5CF6 ${pct*3.6}deg, rgba(255,255,255,.08) 0deg)`}}><div className="grid size-11 place-items-center rounded-full bg-card text-xs font-semibold">{pct}%</div></div>; }
